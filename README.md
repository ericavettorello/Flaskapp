# Flask Simple App with Web Dashboard

🚀 **Простое Flask-приложение с веб-интерфейсом** для демонстрации базовых возможностей веб-фреймворка Flask.

[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](https://www.docker.com/)
[![Flask](https://img.shields.io/badge/Flask-3.0.0-green)](https://flask.palletsprojects.com/)
[![Python](https://img.shields.io/badge/Python-3.10+-blue)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

Простое Flask-приложение с базовыми REST API эндпоинтами, демонстрирующее основные возможности веб-фреймворка Flask.

## 📋 Содержание

- [О проекте](#о-проекте)
- [Что такое Flask?](#что-такое-flask)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [API Эндпоинты](#api-эндпоинты)
- [Docker](#docker)
  - [Образы на Docker Hub](#образы-на-docker-hub)
- [Docker Compose](#docker-compose)
- [Веб-интерфейс](#веб-интерфейс)
- [Переменные окружения](#переменные-окружения)
- [Примеры использования](#примеры-использования)

## 🎯 О проекте

Это учебное Flask-приложение с веб-интерфейсом, созданное для демонстрации базовых возможностей веб-фреймворка Flask. 

**Идеально подходит для:**
- 🎓 Изучения Flask и веб-разработки
- 🚀 Быстрого старта с Docker Compose
- 📚 Демонстрации REST API и фронтенд интеграции
- 🐳 Изучения контейнеризации приложений

Проект состоит из двух компонентов:

### Backend (Flask API)
Приложение предоставляет четыре REST API эндпоинта:

- **Главная страница** (`/`) - информация о доступных эндпоинтах
- **Информация** (`/info`) - метаданные о приложении
- **Health Check** (`/health`) - проверка состояния приложения
- **Калькулятор** (`/calc/<a>/<b>`) - простое сложение двух чисел

Все эндпоинты возвращают данные в формате JSON.

### Frontend (Веб-интерфейс)
Современный веб-дашборд для визуализации и взаимодействия с Flask API:

- 📊 **Dashboard** - отображение информации о приложении в реальном времени
- 💚 **Health Monitor** - мониторинг состояния сервиса
- 🔢 **Интерактивный калькулятор** - удобный интерфейс для вычислений
- 🔗 **Список эндпоинтов** - просмотр всех доступных API маршрутов
- 🔄 **Автообновление** - автоматическая проверка статуса каждые 30 секунд

Веб-интерфейс доступен по адресу `http://localhost:3000` и автоматически подключается к Flask API через внутреннюю сеть Docker Compose.

## 🔧 Что такое Flask?

**Flask** - это легковесный веб-фреймворк для Python, созданный Армином Ронахером (Armin Ronacher) в 2010 году. Flask следует философии "минимализма" и позволяет разработчикам создавать веб-приложения быстро и просто.

### Основные характеристики Flask:

1. **Минималистичность**: Flask предоставляет только базовые инструменты, необходимые для создания веб-приложений
2. **Гибкость**: Разработчик сам выбирает, какие библиотеки и инструменты использовать
3. **Расширяемость**: Большое количество расширений (extensions) для различных задач
4. **Простота**: Легко изучить и начать использовать
5. **WSGI совместимость**: Работает с любым WSGI-совместимым веб-сервером

### Назначение Flask:

- **REST API**: Создание RESTful веб-сервисов и API
- **Веб-приложения**: Разработка полноценных веб-приложений
- **Микросервисы**: Создание легковесных микросервисов
- **Прототипирование**: Быстрое создание прототипов веб-приложений
- **Обучение**: Идеальный инструмент для изучения веб-разработки на Python

### Преимущества Flask:

- ✅ Простой и понятный синтаксис
- ✅ Хорошая документация и большое сообщество
- ✅ Легковесный (минимальные зависимости)
- ✅ Гибкая система маршрутизации
- ✅ Встроенный сервер для разработки
- ✅ Поддержка шаблонов (Jinja2)
- ✅ Работа с сессиями и cookies
- ✅ Интеграция с базами данных через расширения

## 📁 Структура проекта

```
FLASK app/
│
├── app.py                    # Основной файл приложения Flask (Backend)
├── requirements.txt          # Зависимости Python проекта
├── Dockerfile               # Конфигурация Docker образа для Flask
├── Dockerfile.combined      # Объединенный Dockerfile (Frontend + Backend)
├── docker-compose.yml       # Конфигурация Docker Compose (Backend + Frontend)
├── docker-compose.combined.yml # Конфигурация для объединенного образа
├── nginx-combined.conf      # Конфигурация Nginx для объединенного образа
├── .dockerignore            # Файлы, исключаемые из Docker образа
│
├── frontend/                # Веб-интерфейс (Frontend)
│   ├── index.html           # Главная HTML страница
│   ├── style.css            # Стили интерфейса
│   ├── script.js            # JavaScript логика
│   ├── Dockerfile           # Конфигурация Docker образа для Frontend
│   ├── nginx.conf           # Конфигурация Nginx с прокси для API
│   └── .dockerignore        # Файлы, исключаемые из Docker образа Frontend
│
└── README.md                # Документация проекта
```

### Описание файлов:

**Backend:**
- **app.py** - основной файл приложения, содержит все маршруты и логику Flask API
- **requirements.txt** - список Python-пакетов (Flask, flask-cors, Werkzeug)
- **Dockerfile** - инструкции для сборки Docker-образа Flask приложения
- **.dockerignore** - файлы и папки, которые не должны попадать в Docker-образ

**Frontend:**
- **frontend/index.html** - HTML структура веб-интерфейса
- **frontend/style.css** - стили и дизайн интерфейса
- **frontend/script.js** - JavaScript код для взаимодействия с Flask API
- **frontend/Dockerfile** - инструкции для сборки Docker-образа на базе Nginx
- **frontend/nginx.conf** - конфигурация Nginx с проксированием запросов к API

**Docker Compose:**
- **docker-compose.yml** - конфигурация для запуска обоих сервисов (Backend + Frontend) в одной сети

## 📦 Требования

**Для локального запуска:**
- **Python** 3.10 или выше
- **Flask** 3.0.0
- **flask-cors** 4.0.0
- **Werkzeug** 3.0.1 (зависимость Flask)

**Для запуска с Docker Compose (рекомендуется):**
- **Docker** 20.10 или выше
- **Docker Compose** 2.0 или выше

**Для веб-интерфейса:**
- Современный браузер с поддержкой ES6+ (Chrome, Firefox, Edge, Safari)

## 🚀 Установка и запуск

### Локальная установка

1. **Клонируйте репозиторий или перейдите в директорию проекта:**
   ```bash
   cd "FLASK app"
   ```

2. **Создайте виртуальное окружение (рекомендуется):**
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   
   **Windows:**
   ```bash
   venv\Scripts\activate
   ```
   
   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

5. **Запустите приложение:**
   ```bash
   python app.py
   ```

6. **Приложение будет доступно по адресу:**
   - `http://localhost:5001` - Flask API
   - `http://127.0.0.1:5001` - Flask API

**Примечание:** При локальном запуске веб-интерфейс можно открыть напрямую через файл `frontend/index.html` в браузере.

### Запуск без виртуального окружения

Если вы не используете виртуальное окружение, просто установите зависимости и запустите:

```bash
pip install -r requirements.txt
python app.py
```

### Запуск с Docker Compose (рекомендуется)

Для запуска полного приложения с веб-интерфейсом используйте Docker Compose:

**Основной способ (использует объединенный образ из Docker Hub):**

1. **Убедитесь, что Docker и Docker Compose установлены**

2. **Запустите приложение:**
   ```bash
   docker compose up -d
   ```

3. **Приложение будет доступно:**
   - 🌐 Веб-интерфейс: `http://localhost:3000`
   - 🔌 Flask API: `http://localhost:5001`

**Альтернативный способ (локальная сборка отдельных образов):**

Если хотите собрать образы локально:
```bash
docker compose -f docker-compose.build.yml up -d --build
```

**Проверка статуса:**
```bash
docker compose ps
```

**Просмотр логов:**
```bash
docker compose logs -f
```

**Преимущества Docker Compose:**
- ✅ Автоматическая настройка и запуск
- ✅ Веб-интерфейс и API работают вместе
- ✅ Легкое управление и развертывание
- ✅ Изоляция окружения
- ✅ Использует готовый образ из Docker Hub (быстрый запуск)

## 🌐 API Эндпоинты

### 1. Главная страница

**GET** `/`

Возвращает приветственное сообщение и список доступных эндпоинтов.

**Пример запроса:**
```bash
curl http://localhost:5001/
```

**Пример ответа:**
```json
{
  "message": "Добро пожаловать в Flask-приложение!",
  "endpoints": {
    "/": "Главная страница",
    "/info": "Информация о приложении",
    "/health": "Проверка состояния приложения",
    "/calc/<a>/<b>": "Калькулятор (сложение двух чисел)"
  }
}
```

---

### 2. Информация о приложении

**GET** `/info`

Возвращает метаданные о приложении.

**Пример запроса:**
```bash
curl http://localhost:5001/info
```

**Пример ответа:**
```json
{
  "app_name": "Flask Simple App",
  "version": "1.0.0",
  "description": "Простое Flask-приложение с базовыми эндпоинтами",
  "author": "Flask Developer"
}
```

---

### 3. Проверка состояния (Health Check)

**GET** `/health`

Возвращает статус здоровья приложения. Используется для мониторинга и проверки работоспособности сервиса (например, в Docker, Kubernetes, load balancers).

**Пример запроса:**
```bash
curl http://localhost:5001/health
```

**Пример ответа:**
```json
{
  "status": "healthy",
  "service": "Flask Simple App",
  "version": "latest"
}
```

**Использование:**
- Мониторинг состояния приложения
- Health checks в Docker Compose
- Kubernetes liveness/readiness probes
- Load balancer health checks

---

### 4. Калькулятор

**GET** `/calc/<a>/<b>`

Выполняет сложение двух целых чисел.

**Параметры:**
- `a` (int) - первое число
- `b` (int) - второе число

**Пример запроса:**
```bash
curl http://localhost:5001/calc/10/5
```

**Пример ответа:**
```json
{
  "operation": "addition",
  "a": 10,
  "b": 5,
  "result": 15
}
```

**Примеры:**
- `http://localhost:5001/calc/5/3` → результат: 8
- `http://localhost:5001/calc/100/200` → результат: 300
- `http://localhost:5001/calc/-10/5` → результат: -5

## 🐳 Docker

Приложение может быть запущено в Docker-контейнере для изоляции и удобства развертывания.

### Сборка образа

```bash
docker build -t my-flask-app .
```

Эта команда:
1. Скачивает базовый образ Python 3.10-slim
2. Устанавливает зависимости из `requirements.txt`
3. Копирует код приложения
4. Настраивает переменные окружения
5. Создает образ с именем `my-flask-app`

### Запуск контейнера

```bash
docker run -d -p 5001:5001 my-flask-app
```

**Параметры:**
- `-d` - запуск в фоновом режиме (detached mode)
- `-p 5001:5001` - проброс порта (хост:контейнер)
- `my-flask-app` - имя образа

### Полезные команды Docker

**Посмотреть запущенные контейнеры:**
```bash
docker ps
```

**Посмотреть логи контейнера:**
```bash
docker logs <container_id>
# или
docker logs <container_name>
```

**Остановить контейнер:**
```bash
docker stop <container_id>
```

**Удалить контейнер:**
```bash
docker rm <container_id>
```

**Удалить образ:**
```bash
docker rmi my-flask-app
```

**Посмотреть все образы:**
```bash
docker images
```

### Образы на Docker Hub

Готовые образы доступны на Docker Hub и могут быть использованы без локальной сборки:

**Доступные образы:**

1. **🐳 `elena1204/flask-combined:latest`** (рекомендуется) - Объединенный образ с Frontend и Backend в одном контейнере
   - Содержит Flask API и веб-интерфейс
   - Управляется через Supervisor
   - Порт 80 (Frontend) и 5001 (API)
   - **Ссылка:** https://hub.docker.com/r/elena1204/flask-combined

2. **🐳 `elena1204/flask-app:latest`** - Отдельный Flask Backend API
   - Только Flask приложение
   - Порт 5001
   - **Ссылка:** https://hub.docker.com/r/elena1204/flask-app

3. **🐳 `elena1204/flask-frontend:latest`** - Отдельный Frontend веб-интерфейс
   - Только веб-интерфейс на Nginx
   - Порт 80
   - **Ссылка:** https://hub.docker.com/r/elena1204/flask-frontend

**Использование образов из Docker Hub:**

**Вариант 1: Объединенный образ (рекомендуется) - Frontend + Backend в одном контейнере:**

```bash
# Запуск объединенного образа
docker pull elena1204/flask-combined:latest
docker run -d -p 3000:80 -p 5001:5001 elena1204/flask-combined:latest
```

**Или через docker-compose:**
```bash
docker compose -f docker-compose.combined.yml up -d
```

**Вариант 2: Отдельные образы (как в основном docker-compose.yml):**

**Запуск Backend:**
```bash
docker pull elena1204/flask-app:latest
docker run -d -p 5001:5001 elena1204/flask-app:latest
```

**Запуск Frontend:**
```bash
docker pull elena1204/flask-frontend:latest
docker run -d -p 3000:80 elena1204/flask-frontend:latest
```

**Использование в docker-compose.yml:**

**Для объединенного образа:**
```yaml
services:
  flask-combined:
    image: elena1204/flask-combined:latest
    ports:
      - "3000:80"
      - "5001:5001"
```

**Для отдельных образов:**
```yaml
services:
  flask-app:
    image: elena1204/flask-app:latest
    # вместо build: ...
  
  frontend:
    image: elena1204/flask-frontend:latest
    # вместо build: ...
```

**Преимущества использования образов из Docker Hub:**
- ✅ Не нужно собирать образы локально
- ✅ Быстрый запуск без ожидания сборки
- ✅ Готовые к использованию образы
- ✅ Всегда актуальные версии

**Преимущества объединенного образа (`flask-combined`):**
- ✅ Один контейнер вместо двух - проще управление
- ✅ Меньше потребление ресурсов
- ✅ Идеально для простых приложений и быстрого развертывания
- ✅ Все работает в одной изолированной среде
- ✅ Управление процессами через Supervisor

## 🐙 Docker Compose

Для более удобного управления контейнерами рекомендуется использовать Docker Compose. Это позволяет определить все настройки приложения в одном файле `docker-compose.yml`.

### Преимущества Docker Compose:

- ✅ Все настройки в одном файле
- ✅ Простое управление несколькими контейнерами
- ✅ Автоматический перезапуск контейнеров
- ✅ Управление сетями и volumes
- ✅ Удобные команды для управления

### Запуск с Docker Compose

**Запустить приложение в фоновом режиме:**
```bash
docker compose up -d
```

Эта команда:
1. Скачивает объединенный образ `elena1204/flask-combined:latest` из Docker Hub
2. Создает и запускает контейнер `flask-combined-app`
3. Настраивает все переменные окружения
4. Пробрасывает порты:
   - `3000:80` - Веб-интерфейс (Nginx)
   - `5001:5001` - Flask API

**После запуска доступны:**
- 🌐 Веб-интерфейс: `http://localhost:3000`
- 🔌 API напрямую: `http://localhost:5001`

**Примечание:** Основной `docker-compose.yml` использует готовый образ из Docker Hub. Для локальной сборки используйте `docker-compose.build.yml`.

### Полезные команды Docker Compose

**Запустить контейнеры:**
```bash
docker compose up -d
```

**Остановить контейнеры:**
```bash
docker compose down
```

**Посмотреть логи:**
```bash
docker compose logs
```

**Посмотреть логи в реальном времени:**
```bash
docker compose logs -f
```

**Посмотреть логи:**
```bash
docker compose logs flask-combined
# или для локальной сборки
docker compose -f docker-compose.build.yml logs flask-app
docker compose -f docker-compose.build.yml logs frontend
```

**Пересобрать и перезапустить:**
```bash
docker compose up -d --build
```

**Остановить и удалить контейнеры с volumes:**
```bash
docker compose down -v
```

**Посмотреть статус контейнеров:**
```bash
docker compose ps
```

**Остановить контейнеры без удаления:**
```bash
docker compose stop
```

**Запустить остановленные контейнеры:**
```bash
docker compose start
```

**Перезапустить контейнеры:**
```bash
docker compose restart
```

### Структура docker-compose.yml

**Основной `docker-compose.yml` (использует объединенный образ):**

**Сервис `flask-combined`:**
- `image` - использует готовый образ `elena1204/flask-combined:latest` из Docker Hub
- `container_name` - имя контейнера `flask-combined-app`
- `ports` - проброс портов:
  - `3000:80` - Веб-интерфейс (Nginx)
  - `5001:5001` - Flask API
- `environment` - переменные окружения Flask
- `restart` - политика перезапуска (`unless-stopped`)

**Альтернативный `docker-compose.build.yml` (для локальной сборки):**

Содержит два отдельных сервиса:
- **`flask-app`** - Backend (Flask API)
- **`frontend`** - Frontend (Nginx)
- Оба сервиса в одной сети `flask-network` для взаимодействия

**Преимущества основного подхода:**
- ✅ Быстрый запуск (не нужно собирать образы)
- ✅ Один контейнер - проще управление
- ✅ Меньше потребление ресурсов
- ✅ Готовый образ из Docker Hub

### Изменение конфигурации

Для изменения порта, переменных окружения или других настроек отредактируйте файл `docker-compose.yml` и выполните:

```bash
docker compose down
docker compose up -d --build
```

### Пример изменения порта

Отредактируйте `docker-compose.yml`:
```yaml
ports:
  - "8080:5001"  # Порт хоста:Порт контейнера
```

Затем перезапустите:
```bash
docker compose down
docker compose up -d
```

Теперь приложение будет доступно на `http://localhost:8080`.

## 🌐 Веб-интерфейс

Проект включает современный веб-интерфейс для визуализации и взаимодействия с Flask API.

### Возможности веб-интерфейса:

- **📊 Dashboard** - главная панель с информацией о приложении
- **💚 Health Monitor** - отображение статуса здоровья сервиса в реальном времени
- **🔢 Интерактивный калькулятор** - удобный интерфейс для выполнения вычислений
- **🔗 Список эндпоинтов** - просмотр всех доступных API маршрутов
- **🔄 Автообновление** - автоматическая проверка статуса каждые 30 секунд
- **📱 Адаптивный дизайн** - работает на всех устройствах

### Доступ к веб-интерфейсу:

После запуска Docker Compose веб-интерфейс доступен по адресу:
```
http://localhost:3000
```

### Архитектура взаимодействия:

```
Браузер → http://localhost:3000 (Frontend/Nginx)
                ↓
         /api/* проксируется через Nginx
                ↓
         http://flask-app:5001 (Backend/Flask)
```

**Важно:** 
- Запросы от фронтенда к бэкенду идут через внутреннюю сеть Docker Compose
- Nginx проксирует запросы с префиксом `/api/` к Flask API
- CORS настроен в Flask для работы с фронтендом

### Использование веб-интерфейса:

1. **Информация о приложении** - автоматически загружается при открытии страницы
2. **Health Check** - показывает текущий статус сервиса, обновляется автоматически
3. **Калькулятор** - введите два числа и нажмите "Вычислить" или Enter
4. **Список эндпоинтов** - просмотр всех доступных API маршрутов

### Разработка фронтенда:

Для локальной разработки фронтенда без Docker:

1. Откройте `frontend/index.html` в браузере
2. JavaScript автоматически определит локальный режим и будет обращаться к `http://localhost:5001`
3. Убедитесь, что Flask API запущен на порту 5001

## 🔐 Переменные окружения

Приложение поддерживает следующие переменные окружения:

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `PORT` | Порт, на котором будет запущено приложение | `5001` |
| `FLASK_DEBUG` | Режим отладки (`true`/`false`) | `False` |
| `FLASK_APP` | Имя файла приложения (для Docker) | `app.py` |

### Примеры использования:

**Запуск на другом порту:**
```bash
# Windows PowerShell
$env:PORT=8080; python app.py

# Linux/Mac
PORT=8080 python app.py
```

**Запуск с включенной отладкой:**
```bash
# Windows PowerShell
$env:FLASK_DEBUG="true"; python app.py

# Linux/Mac
FLASK_DEBUG=true python app.py
```

**В Docker:**
```bash
docker run -d -p 8080:8080 -e PORT=8080 my-flask-app
```

## 💡 Примеры использования

### Использование с curl

```bash
# Главная страница
curl http://localhost:5001/

# Информация о приложении
curl http://localhost:5001/info

# Проверка состояния
curl http://localhost:5001/health

# Калькулятор
curl http://localhost:5001/calc/15/25
```

### Использование с Python requests

```python
import requests

# Главная страница
response = requests.get('http://localhost:5001/')
print(response.json())

# Информация
response = requests.get('http://localhost:5001/info')
print(response.json())

# Проверка состояния
response = requests.get('http://localhost:5001/health')
print(response.json())

# Калькулятор
response = requests.get('http://localhost:5001/calc/10/20')
print(response.json())
```

### Использование в браузере

**Веб-интерфейс (рекомендуется):**
- `http://localhost:3000` - полный веб-интерфейс с визуализацией

**API напрямую:**
- `http://localhost:5001/` - главная страница API
- `http://localhost:5001/info` - информация о приложении
- `http://localhost:5001/health` - проверка состояния
- `http://localhost:5001/calc/5/3` - калькулятор (результат: 8)

## 🛠️ Технологии

**Backend:**
- **Python 3.10** - язык программирования
- **Flask 3.0.0** - веб-фреймворк
- **flask-cors 4.0.0** - поддержка CORS для фронтенда
- **Werkzeug 3.0.1** - WSGI утилита (зависимость Flask)

**Frontend:**
- **HTML5** - структура веб-интерфейса
- **CSS3** - стили и дизайн
- **JavaScript (ES6+)** - логика взаимодействия с API
- **Nginx Alpine** - веб-сервер для раздачи статики

**Инфраструктура:**
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация нескольких контейнеров
- **Nginx** - веб-сервер и прокси для API

## 📝 Примечания

- Приложение использует встроенный сервер разработки Flask, который **не предназначен для production** использования
- Для production рекомендуется использовать WSGI-серверы, такие как:
  - Gunicorn
  - uWSGI
  - Waitress

### Пример запуска с Gunicorn (production):

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5001 app:app
```

## 📚 Дополнительные ресурсы

- [Официальная документация Flask](https://flask.palletsprojects.com/)
- [Flask на GitHub](https://github.com/pallets/flask)
- [Docker документация](https://docs.docker.com/)
- [Python документация](https://docs.python.org/3/)

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Flask Developer

---

**Версия:** latest  
**Последнее обновление:** 2025

